Optional production audio files:

- `dice-shake.mp3`
- `dice-collision.mp3`
- `dice-stop.mp3`

Current code uses WebAudio synthesis fallback, so gameplay still has sound even when files are missing.
You can replace fallback with these assets later if needed.
